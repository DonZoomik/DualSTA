This is a simple application that is derived from https://learn.microsoft.com/en-us/windows/win32/nativewifi/dual-sta-query-config#enabling-connections-on-the-secondary-sta-interface

It connects the secondary interface and waits forever. If you have an application that could bind to specific IP, you could then use the secondary interface.